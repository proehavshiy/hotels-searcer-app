/* eslint-disable no-unused-vars */
import React from 'react';
import classNames from 'classnames/bind';

import styles from './HotelForm.module.scss';

const cn = classNames.bind(styles);

function HotelForm() {
  return (
    <div>HotelForm</div>
  );
}

export default HotelForm;
